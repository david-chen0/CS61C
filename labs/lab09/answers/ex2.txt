The two implementations are extremely similar in runtime, although the chunks implementation is slightly better. This is likely due to the fact that the memory blocks are contiguous, so there are less cache misses.